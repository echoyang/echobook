<% if flash.any? %>
    <% user_facing_flash.each do |key, value| %>
        <div class="alert alert-<%= flash_class(key) %>">
            <button type="button" name="button" class="close" data-dismiss="alert">x</button>
            <%= value %>
        </div>
    <% end %>
<% end %>
